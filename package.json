{
  "name": "btree-tool",
  "displayName": "%extension.displayName%",
  "description": "%extension.description%",
  "version": "0.0.3",
  "publisher": "local",
  "license": "MIT",
  "keywords": [
    "behavior-tree",
    "groot2",
    "xml",
    "btcpp"
  ],
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:btreeTool.openPreview",
    "onLanguage:xml"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "btreeTool.openPreview",
        "title": "%command.openPreview.title%",
        "icon": {
          "light": "media/preview-light.svg",
          "dark": "media/preview-dark.svg"
        }
      }
    ],
    "menus": {
      "editor/title": [
      {
        "command": "btreeTool.openPreview",
        "group": "navigation"
      }
      ],
      "editor/context": [
        {
          "command": "btreeTool.openPreview",
          "when": "resourceLangId == xml || resourceExtname == .xml",
          "group": "navigation"
        }
      ],
      "editor/tab/context": [
        {
          "command": "btreeTool.openPreview",
          "when": "resourceExtname == .xml",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "btreeTool.openPreview",
          "when": "resourceExtname == .xml",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "package:vsix": "npm test && npx @vscode/vsce package --allow-missing-repository --out btree-tool-v${npm_package_version}.vsix",
    "test": "npm run compile && node --test dist/test/*.test.js tests/*.test.mjs",
    "watch": "tsc -w -p ./",
    "dev:link": "sh ./scripts/install-local-dev.sh",
    "dev:refresh": "npm run compile"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.6.3"
  }
}
